Sources for the plugin `plugin.appodeal.beta.TwitterMoPub`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.TwitterMoPub" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
